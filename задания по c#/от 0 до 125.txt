using System;
class HelloWorld {
  static void Main() {
    int[] array = new int[100];
    Random rand = new Random();
    for (int x = 0; x < array.Length; x++){
          int y = rand.Next(250);
          if((y>0)&(y<125)){
            Console.WriteLine($"{y}");
          }
      }
  }
}